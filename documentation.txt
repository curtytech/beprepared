Link do Tutorial de instalação do nativewind
https://www.nativewind.dev/quick-starts/expo

Criar projeto
npx create-expo-app beprepared

// Instalar NativeWind -- não funciona no powershell
yarn add nativewind

// Criar o arquivo de configuração
npx tailwindcss init

// O Content do tailwind.config.js deve ficar assim
content: ["./App.{js,jsx,ts,tsx}", "./<custom directory>/**/*.{js,jsx,ts,tsx}"],

// O plugins do babel.config.js deve ficar assim
plugins: ["nativewind/babel"],

// Resolver erros do nativewind
npm install


// Intalar Navigation
npm install @react-navigation/native
npx expo install react-native-screens react-native-safe-area-context

// Intalar Stack
npm install @react-navigation/native-stack

// React Icons
npm install react-icons --save


//API
//Instalar Node express
npm install express --save
//Instalar Cors
npm install cors --save

//Sequelize 
npm install --save sequelize mysql2

npm install --save sequelize-cli

npx sequelize-cli init

//Exemplo de criação de migration
npx sequelize-cli model:generate --name User --attributes firstName:string,lastName:string,email:string,idexample:integer,dateexample:date

//Executar Migration
npx sequelize-cli db:migrate

// Exemplo de criação de seeder
npx sequelize-cli seed:generate --name demo-user

//Executar seeder
npx sequelize-cli db:seed:all



//Instalar Nodemon
npm install --save-dev nodemon

//Iniciar Nodemon
npx nodemon run Controller.js

// Instalar asyncStorage Manter variaveis de sessao em cache
npx expo install @react-native-async-storage/async-storage

npm install @react-native-async-storage/async-storage

// Iniciar projeto
npm start


